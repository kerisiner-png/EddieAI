# Аудит ALL — батч 35

Файлы: personality.py, personality_history.py, personality_lifecycle.py
Модель: deepseek-v4-flash

Мы провели аудит трёх файлов: personality.py, personality_history.py, personality_lifecycle.py. Найдены следующие проблемы.

## Файл: identity/personality.py

### 1. Строка 10: `source_types: list[str]` — аннотация типа без импорта `List` из `typing` (или использование `list[str]` требует Python 3.9+). Если проект использует Python <3.9, это вызовет ошибку. Но в целом это не баг, а требование к версии. Отметим как LOW.

### 2. Строка 14: `class PersonalityEngine` — нет docstring, но это не критично.

### 3. Строки 20-24: `CANONICAL_FIELDS` и `SELF_STATE_COLLECTIONS` — множества и словарь, но `SELF_STATE_COLLECTIONS` использует те же ключи, что и `CANONICAL_FIELDS`. Если поле не входит в `CANONICAL_FIELDS`, то обращение к `SELF_STATE_COLLECTIONS[field]` вызовет KeyError. Однако в коде есть проверка `if field not in self.CANONICAL_FIELDS: continue`, так что это безопасно.

### 4. Строка 31: `def __init__(self, pattern_detector):` — нет проверки типа, но это нормально.

### 5. Строки 33-45: метод `candidates` — использует `self.pattern_detector.strong_patterns(...)`. Предполагается, что `pattern_detector` имеет метод `strong_patterns`. Если его нет, будет AttributeError. Это внешняя зависимость, но не баг.

### 6. Строка 47: `field = pattern.category` — предполагается, что у паттерна есть атрибут `category`. Если это не так, будет ошибка. Но это ожидаемо.

### 7. Строки 50-52: `if field not in self.CANONICAL_FIELDS: continue` — корректно.

### 8. Строки 54-70: проверка на существование значения в self_state. Здесь есть потенциальная проблема: `self_state.get(collection, [])` — если `self_state` не имеет метода `get` (например, это не словарь), будет ошибка. Но предполагается, что self_state — это объект с методом get. Также `normalized_current` — множество строк, но `pattern.value.lower().strip()` — строка. Сравнение корректно.

### 9. Строка 72: `candidates.append(PersonalityCandidate(...))` — все поля заполнены. Но `source_types` берется из `pattern.source_types` — предполагается, что это список. ОК.

### 10. Строка 75: `reason` — строка с f-строкой, использует `pattern.count`, `pattern.weighted_score`, `pattern.source_types`. Всё есть.

### 11. Нет обработки случая, когда `pattern_detector.strong_patterns` возвращает None или пустой список — это нормально, вернется пустой список.

### 12. Потенциальная проблема: `pattern.value` может быть не строкой? Если это не строка, то `.lower()` вызовет ошибку. Но предполагается, что value — строка.

### 13. Стиль: длинные строки, но это не ошибка.

### 14. Нет docstring у класса и методов — LOW.

### 15. Возможная утечка: если `pattern_detector` содержит чувствительные данные, они могут попасть в reason. Но это маловероятно.

### 16. Нет обработки ошибок при вызове `strong_patterns` — если он бросит исключение, оно пробросится выше. Это может быть приемлемо.

### 17. В `PersonalityCandidate` поле `source_types` аннотировано как `list[str]`, но если `pattern.source_types` — кортеж или другой итерируемый, то будет ошибка при создании dataclass? Нет, dataclass не проверяет тип, просто присваивает. Так что ок.

### 18. В целом файл выглядит корректно, но есть мелкие замечания по стилю и отсутствию обработки ошибок.

## Файл: identity/personality_history.py

### 1. Строка 1: `from datetime import datetime, timezone` — ок.

### 2. Строка 4: `class PersonalityHistory` — docstring есть.

### 3. Строка 7: `def __init__(self, memory):` — предполагается, что memory имеет атрибут `connection` (объект sqlite3.Connection). Если нет, будет ошибка при вызове `execute`. Это внешняя зависимость.

### 4. Строки 9-31: метод `record` — выполняет INSERT. Потенциальные проблемы:
   - Нет проверки на то, что таблица `personality_history` существует. Если её нет, будет ошибка. Но предполагается, что она создана заранее.
   - Используется `self.memory.connection.execute` — если connection закрыт, будет ошибка.
   - Нет обработки исключений, commit вызывается всегда, даже если execute упал? Нет, если execute бросит исключение, commit не выполнится, но исключение пробросится. Это нормально.
   - Параметры передаются кортежем, всё ок.
   - Нет проверки на длину строк, но это не критично.

### 5. Строки 33-42: метод `recent` — SELECT * ORDER BY id DESC LIMIT ?. Возвращает список строк (sqlite3.Row). Если таблица пуста, вернет пустой список. ОК.

### 6. Строки 44-58: метод `for_trait` — аналогично, но с WHERE. ОК.

### 7. Строки 60-105: метод `trait_summary` — использует `for_trait`, затем вычисляет статистику. Потенциальные проблемы:
   - Если `rows` пуст, возвращает None. ОК.
   - `first = rows[0]`, `last = rows[-1]` — предполагается, что rows отсортированы по id ASC (в for_trait ORDER BY id ASC). Да, так и есть.
   - `event_types` — список строк.
   - `strengths` и `confidences` — списки, но если в rows есть `strength_after` равный None, они отфильтровываются. ОК.
   - `max(strengths)` — если strengths пуст, то max вызовет ValueError. Но здесь есть проверка `if strengths else None`, так что безопасно.
   - `event_types.count("REINFORCED")` и т.д. — если event_types содержит другие значения, они не учитываются. Это нормально.
   - Возвращается словарь, но `evidence_count` берется из `last["evidence_count"]` — если в последней записи оно None, то будет None. ОК.

### 8. Строки 107-131: метод `summary` — если field и value заданы, вызывает for_trait, иначе recent. Затем строит список словарей. Потенциальная проблема: если field задан, а value нет, то условие `if field is not None and value is not None` ложно, и будет вызван recent, что может быть неожиданно. Но это логика метода, возможно, так задумано. Однако если пользователь передал только field, он ожидает фильтрацию по полю, но получит все последние записи. Это может быть багом. Стоит отметить как MEDIUM.

### 9. В методе `summary` нет обработки случая, когда field задан, а value нет — это неоднозначность. Лучше было бы либо требовать оба параметра, либо фильтровать по field отдельно.

### 10. Нет docstring у методов `recent`, `for_trait`, `trait_summary`, `summary` — LOW.

### 11. Используется `self.memory.connection` напрямую, что нарушает инкапсуляцию. Лучше было бы иметь методы в memory для выполнения запросов. Но это архитектурное замечание.

### 12. Нет проверки на то, что `limit` положительное число. Если передать отрицательное, SQLite может вернуть ошибку или все строки. Это LOW.

### 13. В целом файл корректен, но есть логическая проблема в `summary` при передаче только field.

## Файл: identity/personality_lifecycle.py

### 1. Строка 1: `from dataclasses import dataclass, asdict` — ок.

### 2. Строка 2: `from datetime import datetime, timezone` — ок.

### 3. Строка 3: `from typing import Any` — ок.

### 4. Строка 5: `VALID_STATUSES` — множество строк, но не используется в коде. Это мёртвый код? Он объявлен, но нигде не используется. Возможно, предназначен для проверки, но не используется. Отметим как LOW (мёртвый код).

### 5. Строки 8-20: `@dataclass class TraitState` — поля, есть `to_dict`. ОК.

### 6. Строка 22: `class PersonalityLifecycle` — docstring есть.

### 7. Строки 24-28: `ACTIVE_THRESHOLD`, `EMERGING_THRESHOLD`, `WEAKENING_THRESHOLD` — константы.

### 8. Строки 30-40: `__init__` — принимает `self_state` и `history`. Если `self_state` не имеет метода `get` или `set`, будет ошибка. Предполагается, что это объект с такими методами. Также если `personality_traits` отсутствует, он создается пустым словарем. ОК.

### 9. Строки 42-47: `_traits` и `_save_traits` — используют `self.self_state.get` и `set`. ОК.

### 10. Строки 49-54: `_key` — формирует ключ: `f"{field}:{value.strip().lower()}"`. Потенциальная проблема: если `value` не строка, то `.strip()` вызовет ошибку. Но предполагается, что value — строка. Также если value содержит двоеточие, это может привести к коллизиям ключей. Например, field="a", value="b:c" и field="a:b", value="c" дадут одинаковый ключ "a:b:c". Это потенциальная проблема, но маловероятно. Отметим как LOW.

### 11. Строки 56-66: `_status_from_strength` — логика определения статуса. ОК.

### 12. Строки 68-96: метод `get` — возвращает TraitState или None. Использует `data["field"]` и т.д. Если данные повреждены (например, отсутствует ключ), будет KeyError. Но предполагается, что данные корректны.

### 13. Строки 98-141: метод `promote` — создает или обновляет черту. Потенциальные проблемы:
   - Если `existing` не None, то `trait = existing` — это ссылка на объект TraitState, который был создан в `get`. При изменении `trait.strength` и т.д., эти изменения не сохраняются в `self_state` до вызова `_write_trait`. Но `_write_trait` вызывается в конце, так что ок.
   - В `promote` при создании новой черты `status` вычисляется через `_status_from_strength(strength)`, но затем `strength` ограничивается `max(0, min(1, strength))`. Если strength было, например, 1.5, то после ограничения станет 1.0, но статус уже вычислен от исходного значения. Это может привести к несоответствию: статус ACTIVE при strength=1.0 (после ограничения) — это нормально, но если strength было 0.9, статус EMERGING, а после ограничения останется 0.9 — ок. Но если strength было 1.2, статус ACTIVE, а после ограничения 1.0 — тоже ACTIVE. Так что не критично. Но лучше было бы сначала ограничить strength, потом вычислять статус. Отметим как LOW.
   - При обновлении существующей черты: `trait.strength = max(trait.strength, strength)` — это означает, что сила только увеличивается, но не уменьшается. Это может быть нежелательно, если новая оценка ниже. Но это логика promotion — она только усиливает. ОК.
   - Аналогично для confidence и evidence_count.
   - `trait.last_confirmed
