# Аудит ALL — батч 21

Файлы: semantic_claim_auditor.py, semantic_claim_extractor.py, text_relevance_matcher.py
Модель: deepseek-v4-flash

Мы провели глубокий аудит трёх файлов: semantic_claim_auditor.py, semantic_claim_extractor.py, text_relevance_matcher.py. Ниже — детальные находки.

## Файл: core\semantic_claim_auditor.py

### Находки:

1. **Строка 1-2**: `import re` — используется, ок.
2. **Строка 4**: `from core.claim_audit_schema import CLAIM_AUDIT_SCHEMA` — предполагается, что модуль существует. Если нет — будет ошибка импорта. Проверить наличие.
3. **Строка 10-13**: docstring — норм.
4. **Строка 15-...**: SYSTEM_PROMPT — большой текст, содержит инструкции. Обратите внимание: в промпте есть "CANONICAL PREDICATES" и "IMPORTANT". Всё ок.
5. **Строка 100-102**: `def __init__(self, agent):` — принимает agent, сохраняет. Ок.
6. **Строка 104-...**: метод `audit`. 
   - **Строка 107**: `answer = str(answer or "").strip()` — если answer None, станет "". Ок.
   - **Строка 110-116**: если пусто, возвращает структуру с claims=[], structured=True, fallback=False. Ок.
   - **Строка 118**: `sentences = self._split_sentences(answer)` — вызов статического метода.
   - **Строка 120-126**: если нет предложений, аналогичный возврат.
   - **Строка 128-134**: формирует numbered строку с индексами.
   - **Строка 136-141**: audit_input.
   - **Строка 143-...**: try блок.
   - **Строка 144-155**: вызывает `self.agent._generate_structured(...)`. Это внутренний метод агента. Если агент не имеет такого метода — AttributeError. Также передаются параметры: system_prompt, user_prompt, schema, task="conversation", context="", fast=True, num_predict=768. 
     - Возможная проблема: `fast=True` может влиять на качество, но это не баг.
     - `num_predict=768` — может быть недостаточно для длинных ответов, но это не критично.
   - **Строка 156-160**: получает claims из result.get("claims", []). Если result не dict — будет ошибка? result.get — если result не dict, то AttributeError. Но _generate_structured должен возвращать dict, но если он вернёт None или что-то ещё, будет ошибка. Лучше проверить тип.
   - **Строка 161-165**: если claims не list, превращает в [].
   - **Строка 167-173**: возвращает результат.
   - **Строка 175-181**: except Exception — перехватывает все исключения, возвращает fallback. Ок.
7. **Строка 183-...**: статический метод `_split_sentences`.
   - **Строка 188-190**: `re.split(r"(?<=[.!?])\s+", text.strip())` — разбивает по пробелам после точки, восклицательного или вопросительного знака. Проблема: не учитывает сокращения типа "г." или "т.д." — может разбить предложение неправильно. Это может привести к неправильной сегментации, но не критично.
   - **Строка 191-194**: возвращает список непустых частей. Ок.

### Потенциальные проблемы:
- **Строка 144**: вызов `self.agent._generate_structured` — это приватный метод, но используется. Если агент изменится, может сломаться.
- **Строка 144**: параметр `context=""` — пустая строка, возможно, должна быть чем-то заполнена.
- **Строка 144**: `task="conversation"` — возможно, должно быть что-то другое.
- **Строка 144**: `fast=True` — может влиять на качество.
- **Строка 144**: `num_predict=768` — может быть мало для длинных ответов.
- **Строка 144**: не передаётся `temperature` — возможно, нужно.
- **Строка 144**: не передаётся `num_ctx` — возможно, нужно.
- **Строка 144**: не передаётся `think` — возможно, нужно.
- **Строка 144**: не передаётся `options` — возможно, нужно.

### Серьёзность: MEDIUM (зависимость от приватного метода, возможные проблемы с параметрами).

### Как исправить:
- Использовать публичный метод агента, если есть.
- Передавать все необходимые параметры.
- Проверить, что `_generate_structured` возвращает dict.

---

## Файл: core\semantic_claim_extractor.py

### Находки:

1. **Строка 1-2**: `import json, re` — ок.
2. **Строка 4**: `from dataclasses import dataclass` — ок.
3. **Строка 6-14**: dataclass `ExtractedClaim` — frozen=True, поля owner, predicate, value, polarity, certainty, temporal_scope, text. Ок.
4. **Строка 16-...**: класс `SemanticClaimExtractor`.
5. **Строка 18-...**: docstring — ок.
6. **Строка 24-...**: SYSTEM_PROMPT — инструкции. Ок.
7. **Строка 70-...**: `__init__(self, *, model, chat_function)` — принимает model и chat_function. Ок.
8. **Строка 76-...**: метод `extract`.
   - **Строка 78**: `normalized = str(text).strip()` — ок.
   - **Строка 80-82**: если пусто, возвращает [].
   - **Строка 84-...**: вызывает `self.chat(...)`.
     - **Строка 85**: `model=self.model` — ок.
     - **Строка 86-91**: messages — system и user.
     - **Строка 92-96**: options — temperature=0.0, num_ctx=2048, num_predict=512. Ок.
     - **Строка 97**: `think=False` — ок.
   - **Строка 99**: `raw = response["message"]["content"].strip()` — предполагается, что response имеет структуру. Если chat вернёт не так — KeyError.
   - **Строка 101**: `data = self._parse_json(raw)` — вызов статического метода.
   - **Строка 103-...**: цикл по data.
     - **Строка 104**: `if not isinstance(item, dict): continue` — ок.
     - **Строка 106-...**: создание ExtractedClaim.
       - **Строка 107-113**: owner — нормализация.
       - **Строка 114-116**: predicate — нормализация.
       - **Строка 117-119**: value — нормализация.
       - **Строка 120-126**: polarity — нормализация.
       - **Строка 127-133**: certainty — нормализация.
       - **Строка 134-140**: temporal_scope — нормализация.
       - **Строка 141-143**: text — str(item.get("text") or normalized).strip() — если text отсутствует, использует normalized (весь текст). Это может быть неверно: если модель не вернула text для конкретного claim, то подставляется весь входной текст, что может быть избыточно. Лучше использовать пустую строку или что-то типа "". Но это не критично.
   - **Строка 145**: возвращает claims.
9. **Строка 148-...**: статический метод `_parse_json`.
   - **Строка 150**: `cleaned = raw.strip()`.
   - **Строка 152-157**: если начинается с ```, удаляет обрамление.
   - **Строка 159-161**: `json.loads(cleaned)` — если не JSON, возвращает [].
   - **Строка 163-166**: если list — возвращает.
   - **Строка 168-172**: если dict и есть ключ "claims" — возвращает его.
   - **Строка 174**: иначе [].
   - Проблема: если JSON валидный, но не list и не dict с claims — возвращает []. Ок.
10. **Строка 177-...**: статический метод `_normalize_enum`.
    - **Строка 178-184**: value = str(value or "").strip().upper(); если в allowed — возвращает, иначе fallback. Ок.
11. **Строка 186-...**: статический метод `_normalize_predicate`.
    - **Строка 187-...**: value = str(value or "unknown").strip().casefold(); заменяет недопустимые символы на "_", сжимает множественные "_", убирает по краям. Ок.
    - **Строка 196-198**: если пусто — "unknown".
12. **Строка 200-...**: статический метод `_normalize_value`.
    - **Строка 201-203**: если None — None.
    - **Строка 204-206**: str(value).strip() — если пусто, возвращает None? Нет, возвращает ""? Строка 206: `return value or None` — если value пустая строка, то вернёт None. Ок.

### Потенциальные проблемы:
- **Строка 99**: `response["message"]["content"]` — если chat вернёт нестандартную структуру, будет KeyError. Лучше использовать `.get()`.
- **Строка 141-143**: `text` — если модель не вернула text, подставляется весь normalized. Это может привести к дублированию текста в каждом claim. Лучше использовать пустую строку или что-то типа "".
- **Строка 84-97**: вызов chat — если chat_function не поддерживает параметр `think`, будет ошибка. Но это зависит от реализации.
- **Строка 84-97**: не передаётся `stream` или другие параметры — ок.
- **Строка 84-97**: не обрабатывается случай, когда chat возвращает ошибку (например, network). Внешний код должен ловить исключения.

### Серьёзность: MEDIUM (зависимость от структуры ответа, потенциальные KeyError).

### Как исправить:
- Использовать `response.get("message", {}).get("content", "")`.
- Для text использовать `item.get("text") or ""` (или None).
- Обернуть вызов chat в try-except.

---

## Файл: core\text_relevance_matcher.py

### Находки:

1. **Строка 1**: `import re` — ок.
2. **Строка 3-...**: класс `TextRelevanceMatcher`.
3. **Строка 5-...**: docstring — ок.
4. **Строка 10-...**: STOPWORDS — множество стоп-слов. Ок.
5. **Строка 24-26**: `normalize_text` — статический метод, возвращает строку с нормализованным текстом (casefold, split, join). Ок.
6. **Строка 28-...**: `tokens` — classmethod.
   - **Строка 29**: `text = cls.normalize_text(text)`.
   - **Строка 31-33**: `raw = re.findall(r"[a-zа-яё0-9]{4,}", text)` — находит слова длиной >=4 символа. Это может отбросить короткие значимые слова (например, "кот", "дом"), но это допустимо для релевантности.
   - **Строка 35-38**: возвращает set, исключая стоп-слова.
7. **Строка 40-...**: `common_prefix_length` — статический метод.
   - **Строка 41-...**: вычисляет длину общего префикса. Ок.
8. **Строка 51-...**: `token_related` — classmethod.
   - **Строка 52**: `a = a.casefold(); b = b.casefold()`.
   - **Строка 54-56**: если равны — True.
   - **Строка 58-60**: `minimum_length = min(len(a), len(b))`; если <6 — False.
   - **Строка 62-64**: `prefix = cls.common_prefix_length(a, b)`.
   - **Строка 66-68**: `ratio = prefix / minimum
