# Аудит ALL — батч 56

Файлы: test_user_statement_detector.py, test_verbalization_mode.py, test_web_tool.py
Модель: deepseek-v4-flash

## Аудит тестовых скриптов EddieAI

### Файл: `test_user_statement_detector.py`

| № | Строки | Тип проблемы | Серьёзность | Чем грозит | Как исправить |
|---|--------|--------------|-------------|------------|---------------|
| 1 | 1–2 | Нарушение стиля (лишний перенос в импорте) | LOW | Затрудняет чтение, не соответствует PEP8 | Убрать скобки и перенос: `from identity.user_statement_detector import UserStatementDetector` |
| 2 | 9–23 | Отсутствие проверок результатов (нет `assert`) | MEDIUM | Тест не проверяет поведение детектора, ложноположительный проход | Добавить `assert` на ожидаемые результаты для каждого теста |
| 3 | 9–23 | Нет `if __name__ == "__main__":` | LOW | Скрипт выполняется при импорте, может вызвать побочные эффекты | Обернуть исполняемый код в `if __name__ == "__main__":` |

---

### Файл: `test_verbalization_mode.py`

| № | Строки | Тип проблемы | Серьёзность | Чем грозит | Как исправить |
|---|--------|--------------|-------------|------------|---------------|
| 1 | 10–13 | Восстановление состояния через `deepcopy` и `set` в `finally` | HIGH | Если ключи `store.KEY` или `"self_conclusion_state"` отсутствовали до теста, `set` создаст их с пустыми словарями, загрязняя глобальное состояние агента | Сохранять флаг существования ключа и удалять его в `finally`, если его не было |
| 2 | 24 | Использование приватного метода `store._ensure()` | MEDIUM | Приватный API может измениться, тест станет хрупким | Использовать публичный метод инициализации (если есть) или документировать зависимость |
| 3 | 61–74 | Проверка `forbidden_meta` включает слово `"store"` | LOW | Ложное срабатывание, если ответ содержит английское слово "store" (например, "магазин") | Убрать `"store"` из списка или проверять точные метаданные (например, `"self_conclusion_store"`) |
| 4 | 76–84 | Семантические маркеры слишком узкие | LOW | Вербализация может быть корректной, но не содержать ни одного из перечисленных слов (например, "я формирую выводы" – содержит "вывод", но если формулировка иная – тест упадёт) | Расширить список маркеров или использовать более гибкую проверку (например, проверять наличие ключевых понятий из сохранённого заключения) |
| 5 | 157–160 | Восстановление состояния в `finally` (см. п.1) | HIGH | Дублирование проблемы п.1 | Исправить логику восстановления (удалять несуществовавшие ключи) |
| 6 | 161 | Вызов `agent.close()` без обработки исключений | LOW | Если `close()` бросит исключение, оно замаскирует ошибки теста | Обернуть в `try/except` или использовать `contextlib.suppress` |

---

### Файл: `test_web_tool.py`

| № | Строки | Тип проблемы | Серьёзность | Чем грозит | Как исправить |
|---|--------|--------------|-------------|------------|---------------|
| 1 | 10 | Жёстко заданный путь `r"C:\EddieAI"` | HIGH | Тест не переносим на другие ОС (Linux/macOS), вызовет ошибку | Использовать `pathlib.Path` или относительный путь, например `Path(__file__).parent` |
| 2 | 14–20 | Нет обработки ошибок при создании `action` | MEDIUM | Если параметры невалидны, `ActionExecutor.create()` может бросить исключение, тест упадёт без диагностики | Обернуть в `try/except` и логировать ошибку |
| 3 | 22–41 | Нет обработки ошибок при `runner.run()` | MEDIUM | Сетевой сбой или ошибка выполнения приведут к неинформативному падению | Добавить `try/except` и проверку `result["status"]` |
| 4 | 22–41 | Нет закрытия ресурсов `WebExecutor` | MEDIUM | Если `WebExecutor` держит соединения, они не освобождаются | Добавить метод `close()` в `WebExecutor` и вызвать его в `finally` |

---

## Сводка по модулю

Все три файла являются тестовыми
