# Аудит ALL — батч 46

Файлы: patterns.py, provenance.py, research_context.py
Модель: deepseek-v4-flash

## Аудит модулей памяти: patterns.py, provenance.py, research_context.py

### Файл: memory/patterns.py

| Строка(и) | Тип проблемы | Серьёзность | Описание и последствия | Рекомендация |
|------------|--------------|-------------|------------------------|---------------|
| 1-2 | Импорт | LOW | Импорт `EvidenceEngine` из `memory.evidence` — модуль не предоставлен, но предполагается существующим. Если модуль отсутствует или имеет другую структуру, будет `ImportError`. | Убедиться, что модуль `memory.evidence` существует и экспортирует класс `EvidenceEngine`. |
| 17-18 | Инициализация | MEDIUM | `self.evidence = EvidenceEngine(memory)` — нет проверки, что `memory` содержит необходимые атрибуты (например, `connection`). Если `memory` не инициализирован, вызов `all_records()` упадёт. | Добавить проверку наличия `memory.connection` или обработать исключение при создании `EvidenceEngine`. |
| 24-25 | Обработка данных | MEDIUM | `record.last_seen` может быть `None` или не строкой. `datetime.fromisoformat(None)` вызовет `TypeError`, который поймается, но `age_days` станет 0.0, что может исказить расчёт силы паттерна. | Явно проверять `record.last_seen` на `None` и тип, прежде чем парсить. |
| 31-35 | Вычисление возраста | LOW | Используется `datetime.now(timezone.utc)` — корректно. Но если `last_seen` содержит смещение времени (например, `+03:00`), `now - last_seen` будет учитывать разницу, что может дать отрицательное значение, но `max(0.0, ...)` защищает. | Оставить как есть, но добавить комментарий о возможных смещениях. |
| 42-44 | Вычисление decay | LOW | `decay = max(0.0, 1.0 - age_days * DECAY_RATE)` — при больших `age_days` decay станет 0, что корректно. | Без изменений. |
| 47-49 | Создание `Pattern` | HIGH | `record.source_types` передаётся в `Pattern` без проверки типа. Если `source_types` — `None` или не список, будет ошибка при создании dataclass (ожидается `list[str]`). | Добавить проверку: `source_types = record.source_types or []` и привести к списку строк. |
| 53-56 | Метод `strong_patterns` | LOW | Фильтрация по `strength` и `weighted_score` — нет сортировки результатов. Возможен недетерминированный порядок. | Добавить сортировку по `strength` (или `weighted_score`) по убыванию. |
| 53-56 | Метод `strong_patterns` | MEDIUM | Нет ограничения на количество возвращаемых паттернов. При большом количестве записей может вернуть много данных. | Добавить параметр `limit` или срез. |
| 24-25 | Обработка исключений | LOW | В `except` перехватываются `TypeError` и `ValueError`, но не `AttributeError` (если `record` не имеет `last_seen`). | Расширить перехват или проверить наличие атрибута. |

**Сводка по patterns.py:**  
Код в целом корректен, но есть риски, связанные с типами данных из `EvidenceEngine` (особенно `source_types`). Рекомендуется добавить валидацию входных данных и сортировку результатов.

---

### Файл: memory/provenance.py

| Строка(и) | Тип проблемы | Серьёзность | Описание и последствия | Рекомендация |
|------------|--------------|-------------|------------------------|---------------|
| 1-17 | Определение констант | LOW | `VALID_SOURCES` и `SOURCE_WEIGHTS` — множества и словарь. Все ключи `SOURCE_WEIGHTS` присутствуют в `VALID_SOURCES`, что корректно. | Без изменений. |
| 19-22 | `validate_source` | MEDIUM | Если `source` не строка (например, `None` или число), `source not in VALID_SOURCES` может вызвать `TypeError` (для нехешируемых типов) или просто вернуть `False` (для чисел). Это приведёт к `ValueError`, но сообщение может быть неинформативным. | Добавить проверку типа: `if not isinstance(source, str): raise TypeError(...)`. |
| 24-27 | `source_weight` | LOW | Вызывает `validate_source`, затем возвращает вес. Если `source` валиден, всё ок. | Без изменений. |

**Сводка по provenance.py:**  
Код простой и корректный, но стоит добавить проверку типа `source` для надёжности.

---

### Файл: memory/research_context.py

| Строка(и) | Тип проблемы | Серьёзность | Описание и последствия | Рекомендация |
|------------|--------------|-------------|------------------------|---------------|
| 8-10 | `__init__` | MEDIUM | `int(limit)` может вызвать `ValueError`, если `limit` не число (например, строка). Это не обрабатывается. | Обернуть в `try/except` или использовать `isinstance(limit, int)`. |
| 14-15 | SQL-запрос | MEDIUM | Используется f-string для формирования SQL, но параметры передаются через `params` — это безопасно от SQL-инъекций. Однако жёстко закодированы `owner IN ('EXTERNAL', 'SELF')` и `source_type IN ('WEB_SEARCH', 'SELF_INTERPRETATION')`. Если структура БД изменится, запрос сломается. | Вынести условия в константы или параметры. |
| 20-21 | Параметры запроса | LOW | `params.append(self.limit)` — если `limit` не целое, SQLite может выдать ошибку. | Привести к `int`. |
| 24-25 | Выполнение запроса | HIGH | `self.memory
