# Аудит ALL — батч 41

Файлы: user.py, user_evidence.py, user_state.py
Модель: deepseek-v4-flash

## Аудит модуля identity

### Файл: identity/user.py (3 строки)

| Строка | Тип | Серьёзность | Описание | Рекомендация |
|--------|-----|-------------|----------|--------------|
| 1–3 | Стиль | LOW | Словарь `USER_IDENTITY` содержит только имя. Не используется в других файлах модуля (в `user_state.py` имя задано в `DEFAULT_STATE`). Возможно, это мёртвый код или дублирование. | Если не используется — удалить или объединить с `DEFAULT_STATE`. |

---

### Файл: identity/user_evidence.py (41 строка)

| Строка | Тип | Серьёзность | Описание | Рекомендация |
|--------|-----|-------------|----------|--------------|
| 1–41 | Логика | MEDIUM | Класс `UserEvidenceRecorder` принимает объект `evidence` в конструкторе, но не проверяет его наличие и интерфейс. Если `evidence` не имеет метода `add`, будет `AttributeError`. | Добавить проверку типа или явный интерфейс (например, `hasattr(evidence, 'add')`). |
| 15–20 | Стиль | LOW | Метод `record_interest` принимает `value: str`, но затем вызывает `str(value).strip()`. Это избыточно, если тип уже `str`. | Убрать `str()` или изменить аннотацию на `Any`. |
| 22–27 | Логика | MEDIUM | При пустом значении возвращается словарь с `status: "REJECTED"`, но не сохраняется никакой записи. Это нормально, но нет логирования причины. | Добавить логирование (например, `logging.warning`). |
| 29–39 | Логика | MEDIUM | Метод `add` вызывается без проверки результата. Если `evidence.add` вернёт `None` или выбросит исключение, это не обработано. | Обернуть в try/except и проверить возвращаемое значение. |
| 29–39 | Безопасность | LOW | Нет проверки на длину `value` — потенциально можно записать очень длинную строку, что может привести к переполнению БД. | Добавить ограничение длины (например, 500 символов). |
| 29–39 | Консистентность | LOW | Возвращаемый словарь содержит `field: "interest"` — это дублирует категорию. Лучше использовать `category` из `evidence.add`. | Использовать `record.get("category")` или возвращать `category` из параметра. |

---

### Файл: identity/user_state.py (140 строк)

| Строка | Тип | Серьёзность | Описание | Рекомендация |
|--------|-----|-------------|----------|--------------|
| 1–8 | Безопасность | HIGH | Путь к файлу состояния формируется из переменной окружения `EDDIE_DATA_DIR` без проверки на безопасность. Если переменная задана злоумышленником, можно перезаписать произвольный файл (path traversal). | Использовать `Path.resolve()` и проверять, что путь находится в разрешённой директории. |
| 10–11 | Безопасность | MEDIUM | `STATE_PATH` вычисляется один раз при импорте. Если переменная окружения изменится во время работы, это не будет учтено. | Вычислять путь в `__init__` или использовать `os.environ.get` каждый раз. |
| 14–20 | Логика | MEDIUM | `DEFAULT_STATE` содержит `"name": "Эдди"` — это дублирует `USER_IDENTITY`. Если имя изменится в одном месте, другое останется старым. | Использовать единый источник (импортировать из `user.py`). |
| 22–31 | Логика | MEDIUM | В `__init__` при отсутствии файла вызывается `_save(DEFAULT_STATE)`, но `DEFAULT_STATE` — это ссылка на общий словарь. Если позже изменить `self.data`, это не затронет `DEFAULT_STATE`, но если изменить `DEFAULT_STATE` в коде, это повлияет на все экземпляры. | Использовать `copy.deepcopy(DEFAULT_STATE)` при инициализации. |
| 22–31 | Безопасность | MEDIUM | Нет проверки прав доступа к файлу. Если файл существует, но не читается, будет исключение. | Добавить обработку `PermissionError`. |
| 33–40 | Логика | MEDIUM | `_load` не обрабатывает ошибки JSON (например, повреждённый файл). Приведёт к `json.JSONDecodeError`. | Обернуть в try/except и восстановить из `DEFAULT_STATE`. |
| 42–49 | Логика | MEDIUM | `_save` не использует `ensure_ascii=False` в `json.dump`? — используется, но нет `indent`? — есть. Но нет проверки на успешность записи. | Добавить обработку исключений при записи. |
| 51–58 | Логика | LOW | Метод `get` не проверяет, что `self.data` существует (если `_load` упал, `self.data` не определён). | Инициализировать `self.data` в `__init__` до вызова `_load`. |
| 60–67 | Логика | MEDIUM | Метод `set` не проверяет, что `self.data` — словарь. Если `_load` вернул не словарь (например, список), будет ошибка. | Добавить проверку типа. |
| 69–139 | Логика | HIGH | Метод `update_from_message` использует регулярные выражения для извлечения возраста. Проблемы: 1) Паттерн `\bмне\s+(?:уже\s+)?(\d{1,3})\b` может сработать на "мне 25 лет" и "мне 25", но также на "мне 25 рублей" — не проверяется контекст. 2) Паттерн `\bмне\s+(\d{1,3})\s+лет\b` не учитывает склонения ("года", "лет"). 3) Если пользователь скажет "мне 25, а брату 30", будет извлечено только первое. 4) Нет проверки на отрицательные числа (но `\d` не допускает минус). 5) Нет ограничения на количество цифр — `\d{1,3}` допускает "999", но потом проверка `1 <= age <= 120` отсекает. | Улучшить регулярные выражения, добавить проверку контекста (например, наличие слова "лет" или "возраст"). |
| 69–139 | Логика | MEDIUM | После нахождения возраста и изменения, цикл прерывается (`break`). Это правильно, но если пользователь сообщил несколько фактов (например, возраст и имя), они не будут обработаны. | Разделить логику на отдельные методы для каждого типа факта. |
| 69–139 | Логика | MEDIUM | Метод возвращает список изменений, но не сохраняет исходный текст сообщения в `source_text` — он сохраняет полный `message`, что может быть избыточно. | Сохранять только извлечённый фрагмент. |
| 69–139 | Безопасность | MEDIUM | Нет ограничения на размер `message` — потенциально можно передать огромную строку, что замедлит обработку. | Добавить проверку длины. |
| 69–139 | Стиль | LOW | Многострочные регулярные выражения записаны в одну строку с `r"..."` — это нормально, но читаемость страдает. | Разбить на отдельные переменные с комментариями. |
| 69–139 | Логика | MEDIUM | Если пользователь уже сообщил возраст, а потом сообщает другой, метод обновит его. Это может быть нежелательно (например, если это шутка). | Добавить проверку на "подтверждение" или временную метку. |
| 69–139 | Логика | MEDIUM | Метод `update_from_message` не использует `UserEvidenceRecorder` для записи фактов. Это нарушает конвейер evidence→proposal→lifecycle. | Интегрировать запись в evidence при изменении состояния. |
| 69–139 | Логика | MEDIUM | Нет обработки случая, когда `message` — не строка (например, `None`). `message.strip()` вызовет ошибку. | Добавить проверку типа. |
| 69–139 | Логика | MEDIUM | Возвращаемый список `changes` содержит `source_text` — это может быть лишним для больших сообщений. | Сохранять только извлечённый фрагмент. |
| 69–139 | Логика | MEDIUM | Нет проверки на то, что `self.data` существует при вызове `get` внутри цикла. | Убедиться, что `self.data` инициализирован. |
| 69–139 | Логика | MEDIUM | Метод `update_from_message` не сохраняет изменения в `self.data` через `set`? — да, вызывает `self.set`. Но `set` сохраняет весь словарь, что может быть неэффективно при частых вызовах. | Рассмотреть пакетное сохранение. |
| 69–139 | Логика | MEDIUM | Нет обработки ошибок при `int(match.group(1))` — если число слишком большое, `int` может выбросить `ValueError`? Нет, `\d{1,3}` ограничивает, но всё же. | Обернуть в try/except. |
| 69–139 | Логика | MEDIUM | Нет проверки на то, что `old_age` может быть `None` — сравнение `old_age != age` работает, но если `old_age` — строка, будет неожиданное поведение. | Приводить к `int` или `None`. |
| 69–139 | Логика | MEDIUM | Метод `update_from_message` не возвращает информацию о том, было ли изменение (только список изменений). Если изменений нет, возвращается пустой список — это нормально. | Документировать. |
| 69–139 | Логика | MEDIUM | Нет обработки случая, когда `message` содержит несколько возрастов — берётся первый. | Добавить параметр `multiple` или возвращать все найденные. |
| 69–139 | Логика | MEDIUM | Нет проверки на то, что `self.data` — словарь, при вызове `self.get` внутри цикла. | Убедиться в инициализации. |
| 69–139 | Логика | MEDIUM | Нет обработки ошибок при `self.set` — если запись не удалась, изменения не будут сохранены, но метод вернёт `changes`. | Обернуть `set` в try/except и вернуть ошибку. |
| 69–139 | Логика | MEDIUM | Метод `update_from_message` не использует `UserEvidenceRecorder` для записи фактов. Это нарушает конвейер evidence→proposal→lifecycle. | Интегрировать запись в evidence при изменении состояния. |
| 69–139 | Логика | MEDIUM | Нет обработки случая, когда `message` — не строка (например, `None`). `message.strip()` вызовет ошибку. | Добавить проверку типа. |
| 69–139 | Логика | MEDIUM | Возвращаемый список `changes` содержит `source_text` — это может быть лишним для больших сообщений. | Сохранять только извлечённый фрагмент. |
| 69–139 | Логика | MEDIUM | Нет проверки на то, что `self.data` существует при вызове `get` внутри цикла. | Убедиться, что `self.data` инициализирован. |
| 69–139 | Логика | MEDIUM | Метод `update_from_message` не сохраняет изменения в `self.data` через `set`? — да, вызывает `self.set`. Но `set` сохраняет весь словарь, что может быть неэффективно при частых вызовах. | Рассмотреть пакетное сохранение. |
| 69–139 | Логика | MEDIUM | Нет обработки ошибок при `int(match.group(1))` — если число слишком большое, `int` может выбросить `ValueError`? Нет, `\d{1,3}` ограничивает, но всё же.
