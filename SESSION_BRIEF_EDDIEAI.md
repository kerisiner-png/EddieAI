# SESSION BRIEF — сессия EddieAI (параллельно с интерфейсной сессией)

Дата создания: 22.08.2026 вечер. Выдала: интерфейсная сессия
(dashboard/music/konoha-map), работающая в simulation_framework.

Твоя зона: ТОЛЬКО репозиторий `C:\EddieAI` (агент).
Интерфейсная сессия пишет только в
`C:\EddieAI_Simulations\simulation_framework\dashboard\*`.
Кодовых пересечений нет. Не выходи из своей зоны.

## Порядок входа (строго)

1. Прочитай `C:\EddieAI\AGENTS.md` целиком.
2. Прочитай `C:\EddieAI\PROJECT_STATE.md` целиком.
3. Зарегистрируйся: допиши короткую запись о себе (кто/задача/время)
   В КОНЕЦ `C:\EddieAI_Simulations\simulation_framework\COORDINATION.md`
   (read + append, НЕ перезаписывать файл).

## Первая задача — до любой новой работы

Ревизия незакреплённой работы (проблема №2 HIGH из реестра):
- в `C:\EddieAI` лежат ~20 файлов core/identity/data с
  незакоммиченными изменениями поверх коммита `5f28a25`;
- выполни `git status`, `git diff --stat`, затем содержательный
  разбор диффа по файлам;
- сведи результат в таблицу: файл → суть изменения → риск →
  рекомендация (коммит / откат / разделить);
- ПРЕДСТАВЬ пользователю на решение. Самовольно не коммить и
  не откатывай (коммиты — только по явной просьбе пользователя).

Пока ревизия не решена — НИКАКИХ новых правок поверх текущего
состояния, иначе диффы перемешаются безвозвратно.

## Красные линии

1. DecisionRuntime / LLM-интеграцию / мост — не трогать без
   отдельного решения пользователя.
2. Данные личности (`data/*.db`, `self_state.json`,
   `user_state.json`, cognitive_queue.json) — не менять руками;
   только легитимные конвейеры (evidence → proposal → lifecycle)
   и только после бэкапа.
3. Кодировки: UTF-8 БЕЗ BOM; никаких правок исходников через
   PowerShell Set-Content/Out-File/Add-Content; после правок —
   байтовая проверка (литеральные «?», «Ð»-мохибек).
4. Коммиты — только по явному запросу пользователя.

## Ресурсы машины (8 ГБ RAM!)

- ollama + llama-server УЖЕ запущены и прогреты
  (phi4-mini, qwen3.5:4b) и живут до ~21:30 по решению
  пользователя. ИСПОЛЬЗУЙ их; вторую копию модели НЕ поднимай.
- После 21:30 — правило «запустил → останови сам»;
  перед завершением сессии проверь:
  `Get-Process ollama,llama-server` — согласуй с пользователем.
- Не держи одновременно: браузер + тяжёлый E2E-прогон + лишние
  окна (утренний урок: node-агенты падали от нехватки памяти).

## Текущий контекст задач (из PROJECT_STATE.md, раздел 4)

- Цель совета: «Работающий голос». T0–T2 завершены
  (отчёт `..\EddieAI_Simulations\отчеты\VOICE_DIAGNOSIS_T0T2.md`).
- T3–T6 ждут решения совета: включать ли appraisal наблюдений
  в объём фазы. Если пользователь скажет «продолжай голос» —
  сначала подтверди у него объём T3, потом работай.
- Параллельно интерфейсная сессия делает редизайн дашборда
  симуляции — её задачи тебя не касаются.

## Отчётность

- Содержательные этапы — краткой записью в COORDINATION.md
  (append) и обновлением разделов 3–5 PROJECT_STATE.md.
- Язык всего — русский. Врём нельзя: не сделал — так и скажи.
