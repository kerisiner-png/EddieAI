# План: документация + визуальный пакет «Старинный свиток»

Утверждено пользователем 22.08.2026 («да, давай»). Исполнение —
после выхода из планового режима.

## Блок 1 — документация

1. `C:\EddieAI\docs_engineer\reports\2026-08-22_вечер-ночь_дашборд-v2_и_документация.md`
   — отчёт смены (содержимое готово в истории сессии).
2. README.md (docs_engineer): строка в карту таблицы —
   `reports\ | Отчёты смен EddieAI-репо | По итогам смены`;
   формат имени отчёта: ГГГГ-ММ-ДД_смена_тема.md.
3. ROADMAP.md: в «Сменный фронт» ссылку на отчёт смены.
4. COORDINATION.md + PROJECT_STATE.md: краткие отметки о смене
   (Read перед Edit обязателен).
5. Байт-проверка всех файлов docs_engineer + AGENTS.md:
   UTF-8 без BOM, нет «?»-литералов, нет «Ð»-мусора.

## Блок 2 — konoha_map.py

Методы + вставки в render():
- `_draw_aging(nf)`: 11 пятен старения (stipple gray12/gray25,
  позиции детерминированы _digest("stainN"), цвет mix("#C9AE79",
  PALETTE["ground_night"], max(0.55,nf))) + 6 трещин-прожилок
  (ломаные 6 сегментов, width=1). Слой: после травы, до парков/воды/
  домов. Проверка наличия ключей палитры перед использованием.
- `_draw_grid(nf)`: сетка i=1..9 по ширине/высоте канвы, отступ рамки
  14px, stipple gray12, цвет mix("#B49A66", ground_night, max(0.45,nf)).
- `_draw_map_furniture(nf)`: двойная рамка (внешняя width=3 road_edge,
  внутренняя width=1 ink, m=14, вторая m+5); компас-роза справа внизу
  (cx=w-74, cy=h-84, R=34: 4 длинных луча polygon попеременно
  ink/ink_soft + 4 диагональные линии, буква N Georgia bold сверху);
  штамп названия посёлка слева сверху (Georgia bold 15, ink,
  двойное подчёркивание len*9px). Слой: после всех объектов, до
  ночного тинта (гаснет ночью автоматически).

## Блок 2 — live.py

- Статусная строка низа окна: grid row=3, height=22, bg colors["black"];
  слева run-id/seed/NPC (StringVar, обновление в _refresh), справа
  музыкальный режим; sidebar rowspan скорректировать (не перекрывать).
- Хоткеи Ctrl+1..8: root.bind_all("<Control-Key-n>"), индекс →
  TAB_NAMES[i], вызов существующего show_tab/_select_tab.

## Блок 3 — верификация и сдача

1. py_compile live.py + konoha_map.py; import обоих.
2. Смоук рендера карты: счёт объектов канвы вырос, пятна/сетка/рамка/
   компас/штамп присутствуют (перечислить теги/типы items).
3. Перезапуск демо через WMI; PrintWindow-скрин как артефакт.
4. Байт-проверка правленых исходников (BOM/mojibake/q-runs).
5. CHANGELOG.md: запись о пакете; TODO.md: пункт пакета → [АРХИВ].
6. Финал: winsound.Beep сигнал пользователю, краткий отчёт:
   файлы/проверки/риски. Процессов не оставлять (учесть детач-плеер).

## Риски

- Ключи палитры могут отличаться от memory (#B99B62/#755A37) —
  сверить konoha_theme.py перед кодом.
- Grid row=3 может конфликтовать с текущей конфигурацией строк root —
  прочитать _build заново перед правкой.
- Кириллица в штампе через Python-код безопасна (файл UTF-8),
  но проверять байт-чеком после правок.
