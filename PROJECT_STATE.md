# PROJECT_STATE — живое состояние системы EddieAI

Обновлён: 23.08.2026 день. Ведёт: главный инженер.
Правило: перед работой прочитай этот файл целиком; после содержательной
задачи — обнови разделы 3–5.

> **ЭПОХА 2 (23.08.2026, решение совета директоров): память личности
> обнулена** — мусор до-системной эпохи стёрт. Архив Эпохи 1:
> `data_archive_epoch1_2026-08-23/` (SHA256-сверен), тестовые артефакты:
> `data_test_artifacts_epoch1_2026-08-23/`. Текущее прод-состояние:
> memory.db с 3 событиями (#1,#2 первый диалог, #3 SYSTEM/BIRTH_EPOCH2),
> self_state/user_state = DEFAULT от IDENTITY_SEED. Изоляция симуляции:
> полная (EDDIE_DATA_DIR, профиль в simulation_framework/eddie_data_profile/,
> прод не открывается симуляцией ни на чтение ни на запись; тест
> ISOLATION_TEST_PASS). Разделы ниже частично описывают Эпоху 1 —
> сверяй даты.

---

## 1. Что это за система

Строится искусственная развивающаяся личность (EddieAI, «цифровой
ребёнок», возраст в self_state: 12). Имени у агента НЕТ и не должно
быть, пока он сам его не выберет [решение создателя]. «Эдди» — это
человек-создатель (user_state.json). LLM — орган речи/рассуждения;
личность — персистентные состояния + легитимные конвейеры их изменения:
evidence → proposal → decision → promotion (жизненный цикл черт
CANDIDATE→EMERGING→ACTIVE→WEAKENING→DORMANT→REJECTED).

Две системы:
- **C:\EddieAI** — агент: core\ (мозг, 5169-строчный Agent, автономный
  цикл, фоновое познание), identity\ (личность), memory\ (SQLite),
  инструменты llm/filesystem/web/research.
- **simulation_framework** — среда: процедурный мир (266 NPC, время 1:1,
  погода, экономика, социум, нормативные реакции с эскалацией),
  headless-драйверы и дашборд.

Мост: мир сплющивается в текст → `Agent.respond(str)` → ответ →
ActionObserver строковыми шаблонами вытаскивает действия → последствия
в мире. Память агента на прогон изолируется песочницей (копия memory.db).

## 2. Верифицированное состояние (на что можно опираться)

[Подтверждено прогонами 21–22.08.2026]
- Полный E2E симуляции С LLM: `runs/truancy_morning_f91d79e590`
  (100 мин мира, finished=True failed=False, 0 критических ошибок).
- Эскалация нормативных реакций работает: замечание → повтор → звонок
  родителю → разговор при явке; Эдди получает реакции через мост.
- День мира эталонен (900 тиков): школа → работа → дом к 21:26,
  потребности эволюционируют.
- Агент в прогонах: 10 LLM-циклов, переезд home→school, получение
  социальных реакций. Данные: self_state.json (interest астрофизика
  ACTIVE 0.84, values, beliefs), memory.db (events ×1693).

## 3. Roadmap (структура утверждена создателем; статусы верифицированы инженером 22.08)

| Этаж | Смысл | Статус (верифицирован) |
|---|---|---|
| 0 | Среда | DONE* (Git только у агента; Ollama нестабильна) |
| 1 | Первичное ядро | DONE (seed содержателен; BOM в seed - починить) |
| 2 | Память | DONE (USER/SELF/EXTERNAL/SHARED подтверждены; retrieval под нагрузкой не тестировался) |
| 3 | Самосогласованность | МЕХАНИЗМ ГОТОВ, ИСПОЛНЕНИЕ ТЕЧЁТ (боевой E2E: нарушения промпт-правил, метаречь qwen 10/10) |
| 4 | Зародыш саморазвития | DONE как зародыш (конвейер прошёл end-to-end по данным) |
| 5 | Личность | **~65–70%** (не 90–95%): preferences/habits пусты, взросление и собственная речь отсутствуют целиком; интересы/эмоции/history живые |
| 6 | Мотивация | СЛЕДУЮЩИЙ КРУПНЫЙ (каркас уже стоит - углубление) |
| 7–15 | далее по роадмапу | НЕ НАЧАТЫ (diary/sleep/dream/aging - 0 вхождений в коде) |

Инженерные заметки к роадмапу:
- Этаж 5 блокирован гигиеной: «?»-мусор в reason идентичности (проблема №1)
  + BOM уже в identity_seed.py;
- Проверить decay аффекта: frustration упёрся в 1.0 и держится;
- Цель из продакшн-data создана и завершена за 2 минуты (тестовый
  артефакт в живых данных) - агенту нужен реальный жизненный опыт;
- Каркас этажа 6 частично существует - работа будет углублением;
- Этажи 7–8 потребуют обогащения канала моста с симуляцией;
- Этаж 13 требует предварительного проекта песочницы инструментов
  (FilesystemExecutor уже может писать в собственные исходники).

## 4. Реестр проблем (приоритет HIGH→LOW)

1. **HIGH | Кодировки** — болезнь поразила ОБА проекта: литеральные «?»
   в reason-строках identity-предложений (`core/agent_loop.py` ~296),
   двойное перекодирование описаний инструментов (`autonomy_runtime_factory.py`),
   `bridge.py:233` source_detail «?????» попадает в промпты/журнал.
   Источник: PowerShell-инструменты правки. Требуется: реестр поражений
   байтовым сканом + правило кодировок (уже в AGENTS.md) + починка по
   приоритету влияния на данные личности.
2. **HIGH | Незакреплённая работа** — ~20 файлов core/identity/data с
   незакоммиченными изменениями поверх коммита 5f28a25. Нужна ревизия
   диффа и решение пользователя: коммит/откат.
3. **MEDIUM | simulation_framework без VCS** — предложен git init,
   ожидает решения пользователя (самовольно репозиторий не создавать).
4. **MEDIUM | Качество LLM-ответов** — qwen3.5:4b в E2E отвечал
   метаречью мимо формата (10/10 циклов уходили в «астрофизику» или
   сервисные шаблоны). Диагностика промптов/роутинга не проводилась.
5. **MEDIUM | Бедный сенсомоторный канал** — ActionObserver = 5
   строковых шаблонов; богатый внутренний мир сплющивается в текст.
   Стратегический фронт, трогать только эволюционно.
6. **MEDIUM | data/ захламлена** — продакшн memory.db рядом с десятками
   тестовых db/json, cognitive_queue.json 3 МБ без ротации. Разделить.
7. **LOW | Монолит agent.py** — 5169 строк, роутинг entangled. Не
   переписывать; документировать при касании.
8. **LOW | Файл-плейсхолдеры пусты** (emotions/, goals/, world/, tools/,
   config/...) — функциональность фактически в identity/ и core/.
9. **LOW | BOM** в checkpoints.py, verify_normative_eddie_loop.py —
   косметика, Python терпит.

## 4. Очередь задач

Утверждено советом (22.08): цель **«Работающий голос»**.
- Выполнено T0–T2 (диагноз, без правок кода агента):
  базлайн 2/10=20% детерминистики (~0% честно), адресованность 0/10;
  корень — наблюдения мира идут как GENERAL_QUERY (роутер не знает
  канал мира) + мусор bridge.py:233 в каждом промпте; guard'ы полируют
  длину, не персону; канал контаминации evidence подтверждён.
  Отчёт: ..\отчеты\VOICE_DIAGNOSIS_T0T2.md. Инструмент: voice_checker.py.
- Ожидает решения совета: включать ли appraisal наблюдений в объём
  фазы (или сузить DOWNSTREAM-критерий до «записаны+чистота»);
  затем T3–T6 (маршрут WORLD_OBSERVATION, мусор моста, пост-валидатор,
  A/B до ≥80%).

Предложено ранее, ожидает своей очереди:
- Б. Журнал развития личности (трассировка proposal→decision→promotion).
- D. Структурирование контракта моста (наблюдение/действие).
- Следующая крупная цель после голоса: «Первый прожитый день».

### ОБНОВЛЕНИЕ 23.08 день: утверждён рубеж «Первое собственное действие»
Решение Эдди лично: «Первый прожитый день» НЕ следующая задача; сначала
замкнуть петлю мир→наблюдение→решение→действие→последствие→память→
appraisal→следующий цикл. Схема: «ядро решает — модель только
вербализует» (AgentLoop+ActionSelector пробуждается, LLM = орган речи).
План: R0 предохранители [ЗАКРЫТ 23.08, см. CHANGELOG] → R1 канал
действия ядра → R2 «Проба воли» (3 прогона) → A/B вербализаторов →
R3 режимы жизни 24/7 (сон/пробуждение; игры Эдди — Minecraft).
Детали фаз: docs_engineer\TODO.md. Закрыты находки аудита №2–№8(част.),
№17(част.), №24; открытые остатки: №1(хвост agent.py LOW), №9, BOM-зачистка.

## 5. Журнал значимых событий

- 24.08 ~16:00: core/agent.py ПОЛНОСТЬЮ восстановлен после катастрофы
  `git checkout --` (13:48): найден shadow-git opencode с 49 снимками
  файла, установлен последний здоровый 77a08896 (6040 строк, 13:46:17).
  Все проверки PASS; бэкапы в agent_py_recovery_2026-08-24\. Файл снова
  незакоммичен — решение о коммите за Эдди. См. CHANGELOG/MEMORY.
- 24.08 13:48: катастрофа — mistral-large выполнила `git checkout --
  core\agent.py`, стерев ~2 дня правок (файл откатился до древних 857
  строк). Сессия восстановления оборвалась из-за интернета на стадии
  гибрида с 993 строками дыр; завершена следующей сменой (см. выше).

- 22.08 ночь: стабилизация симуляции подтверждена E2E с LLM; внедрена
  эскалация нормативных реакций (NIGHT_SHIFT_REPORT.md в ..\отчеты\);
  устранена ночная порча файлов causal.py/checkpoints.py (неизвестным
  процессом 03:51–03:52); остановлен забытый llama-server (2.5 ГБ).
- 22.08 утро: анализ полного EddieAI выполнен (см. отчёт в беседе от
  22.08); главный инженер принят в совет директоров; создана эта
  система персистентного контекста.
- 22.08 день-вечер: дашборд симуляции — HUD поверх карты Конохи v1.5
  (статусбар: часы мира/фаза дня/погода/счётчики «кто где»; лента
  событий «Хроника посёлка» с цветными маркерами по типу) и музыкальный
  движок v2 (рендер в фоновом потоке — фризы UI устранены, бесшовный
  луп, кэш с автоочисткой). Детали и проверки: COORDINATION.md симуляции.
  Находки: BOM в dashboard/live.py (был до правок, ждёт решения);
  ollama/llama-server оставлены работать до 21:30 по лимиту.
- 22.08 вечер (продолжение): редизайн дашборда — этапы А/Б готовы:
  управление картой Конохи починено (зум/панорама/поиск), карта
  обогащена по рефам пользователя (скала Хокаге, парки, пруды,
  дым/окна/флаги), боковая панель-навигация вместо верхних вкладок,
  музыкальные фризы устранены. LLM-стек остановлен досрочно решением
  пользователя (~17:00); симуляция планируется к ~23:00 — стек поднять
  заново под прогон. Глубокая полировка вкладок ждёт остальные рефы
  (..\EddieAI_Simulations\рефы\).

- 22.08 вечер (2): создана инженерная документация
  C:\EddieAI\docs_engineer\ (README/ROADMAP/TODO/CHANGELOG/MEMORY/
  ASSUMPTIONS/reports\) с разделением записей [АКТУАЛЬНО]/[АРХИВ];
  AGENTS.md дополнен целью проекта и составом совета директоров;
  реализован утверждённый по рефам визуальный пакет: пергамент-декор
  карты (пятна/трещины/сетка/рамка/компас/штамп посёлка), статусная
  строка низа окна дашборда, хоткеи Ctrl+1..8. Проверки: compile/
  import/смоук рендера PASS, демо перезапущено, скрины сняты.
  Отчёт смены: docs_engineer\reports\2026-08-22_вечер-ночь_*.md.

- 22.08 ночь (закрытие смены): пакет «Старинный свиток» реализован и
  принят Эдди как рабочая база; статусная строка низа + хоткеи
  Ctrl+1..8 в дашборде; BOM live.py очищен; вердикт Этажа 5 — канон
  ~65–70% (решение делегировано инженеру, аудит артефактами в
  docs_engineer). Приёмка: интерфейс в целом «не красиво и схематично»,
  карта — схема-база; утверждён фронт UI Beauty Pass всего дашборда.
  НОВЫЙ БАГ HIGH: генерация музыки не работает (один старый кэш-wav
  лупится, новых рендеров нет, лог чист) — факты и гипотезы в
  docs_engineer\CHANGELOG.md и TODO.md. Демо/плееры погашены,
  python-процессов 0, Ollama не запущена.
- 22.08 вечер/ночь (соло, мандат «работай сам ~час»): дашборд получил
  чиптюн-звуки v3 (NES-каналы: квадрат duty/треугольник/шум,
  хирадзёси-пентатоника; API прежний, кэш *_v3.wav) и анимационный
  пакет live.py (тлеющая линия сайдбара, вспышка рамки контента при
  смене вкладок, сюрикен-спиннер по статусу прогона, плавный твининг
  шкал отношений). Смоук на реальном рантайме без LLM: обход 8 вкладок,
  PrintWindow-скрины 8/8 (%TEMP%\opencode\ui_beauty_pass\), пиксельные
  пробы темы PASS; визуальная приёмка за Эдди (у инженера нет vision).
  Заявлен новый фронт «юзабилити дашборда» (TODO п.4). Баг музыки НЕ
  трогался. Изменённые файлы: dashboard/sounds.py (переписан),
  dashboard/live.py (минимальные диффы); смоук-run удалён;
  ollama/llama-server 0 процессов.

## 6. Как запускать (шпаргалка)

```
# полный E2E симуляции с LLM (Ollama должна работать!):
python C:\EddieAI_Simulations\simulation_framework\run_night_scenario.py truancy_morning 6000

# прокрутка дня мира без LLM (~2 мин):
python C:\EddieAI_Simulations\simulation_framework\scroll_truancy_no_llm.py

# лестница эскалации (юнит):
python C:\EddieAI_Simulations\simulation_framework\verify_normative_escalation.py

# диалог с агентом в консоли:
python C:\EddieAI\main.py
```

После работы с LLM: проверить `Get-Process ollama,llama-server` —
должно быть пусто.

## Бюджет и приоритеты (решение Эдди, 23.08.2026 вечер)

- **Бюджет проекта: 0 рублей.** Жёсткое ограничение: только бесплатные
  сервисы и локальные модели.
- **Приоритет: голосовой этап раньше ночи R2.** EddieAI должен заговорить.
- Российские LLM-сервисы (GigaChat и пр.) НЕ рассматриваются — решение Эдди.
- Выбрана архитектура «двойное дыхание»: тело/память/рост локально,
  живой голосовой канал — через бесплатный облачный «рот» (кандидаты
  проверяются через DDG MCP после рестарта opencode).
- Референс устройства — AI-втуберы (Neuro-sama): уши whisper, рот edge-tts/
  silero, дирижёр разговора (turn-taking) — будущий модуль, фильтр перед ртом
  обязателен. Каркасы втуберов целиком НЕ берём — только идеи органов.



### Текущее состояние EddieAI (вечер 24.08.2026)

Стек живого голоса (voice_repl.py):
уши voxtral-mini (облако, fallback whisper medium лениво) ->
мозг mistral-small-latest (облако, fallback qwen локально) ->
рот z3-конвейер (edge-tts Svetlana + DSP + PSOLA, голос пацана 12 лет,
утверждён Эдди) -> sounddevice. Автономия подключена
(AutonomyRuntimeFactory, тик 300с, max 3/час).

Открытые проблемы (план P1-P6 согласован):
P1 few-shot честности (фантомы биографии, эскалация лжи);
P2 семантический судья ответа (уклонения, фабрикация - строки бессильны);
P3 память диалога limit 3->6 (мотивационный сброс контекста);
P4 разговорные цели -> GoalManager (слова должны становиться делом);
P5 стартовое наполнение self_state из seed (меньше дыр для фантазий);
P6 ритуал снимков души до/после сессии.
Наблюдение: податливость на «ради меня» (нет воли отказывать); вопрос о
личности («от тебя или от модели?») остаётся без ответа - граница строковых
валидаторов.
